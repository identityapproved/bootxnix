# Nix build outputs
result
result-*

# direnv
.direnv

# per-box secret: the actual key list (commit the .example only)
secrets/ssh-keys.nix

# committed flake.lock is fine; ISO images and reference clones are not
*.iso
refs/
